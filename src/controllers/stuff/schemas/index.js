const { postStuffSchema } = require('./post-stuff');
const { loginStuffSchema } = require('./login-stuff');
const { patchStuffSchema } = require('./patch-stuff');

module.exports = {
  postStuffSchema,
  loginStuffSchema,
  patchStuffSchema,
};
